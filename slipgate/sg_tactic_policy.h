#ifndef SG_TACTIC_POLICY_H
#define SG_TACTIC_POLICY_H

#include "sg_tactic_contract.h"

/* A candidate first proves `canonical successor cost + exact authenticated
 * live Q52.12 fiber cost < canonical source field cost` for the exact v12
 * successor state. Authenticated modifiers then deform only ranking; they
 * cannot admit, reject, or alter the published nominal cost. Equal ranks
 * prefer lower nominal cost, higher descriptor priority, lower capability,
 * lower target phase, then earlier descriptor. Every eligible descriptor is
 * still probed once before the winner is emitted. */

#endif /* SG_TACTIC_POLICY_H */
